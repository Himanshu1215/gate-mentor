# GPU-VM-only requirements for fine-tuning Phi-4-mini (QLoRA).
# Do NOT install these on the 8 GB CPU app VM — they pull a CUDA torch build.
#
# Install on the GPU VM (CUDA 12.x):
#   python -m venv venv && source venv/bin/activate
#   pip install -r train/requirements-gpu.txt
#
# If torch does not pick up CUDA automatically, install it first from the
# official index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

torch>=2.3.0
transformers>=4.45.0
datasets>=2.20.0
accelerate>=0.34.0
peft>=0.12.0
trl>=0.11.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
protobuf>=4.25.0
